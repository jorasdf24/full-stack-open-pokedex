name: Hello World!

on:
  push:
    branches:
      - main

jobs:
  hello_world_job:
    runs-on: ubuntu-latest
    steps:
      - name: Say hello
        run: |
          echo "Hello World!"
  now_it_is_job:
    runs-on: ubuntu-latest
    steps:
      - name: Now it is
        run: date
  content_job:
    runs-on: ubuntu-latest
    steps:
      - name: Directory content
        run: ls -l

